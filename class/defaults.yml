parameters:
  metallb:
    namespace: syn-metallb
    secretkey: none 
    # ?{vaultkv:${customer:name}/${cluster:name}/metallb/secretkey}
    # kubectl create secret generic -n metallb-system memberlist --from-literal=secretkey="$(openssl rand -base64 128)"
    protocol: layer2
    addresses: 
      - x.x.x.x/32
