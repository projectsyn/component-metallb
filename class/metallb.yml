parameters:
  kapitan:
    compile:
      - input_paths:
          - metallb/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - metallb/component/main.jsonnet
        input_type: jsonnet
        output_path: metallb/
